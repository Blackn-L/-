``` JavaScript
1、arr.indexOf()  # 找到给定元素的第一个索引，无则返回-1 
2、arr.forEach(function(当前值，当前索引，数组) {})# 遍历数组，接收一个回调函数，无返回值
3、arr.map(function() {}) # 根据函数方法，返回一个处理后的新数组
4、arr.filter(function() {}) # 根据函数方法，返回一个符合条件的新数组
5、arr.some(function() {}) # 根据函数方法，返回一个布尔值，只要满足一个，就返回true
6、arr.every(function() {}) # 根据函数方法，只有全部满足，才返回true
```